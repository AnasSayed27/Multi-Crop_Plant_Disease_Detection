# =====================================================================
# Multi-Crop Plant Disease Detection System — Environment Configuration
# =====================================================================

# Server Configuration
HOST=0.0.0.0
PORT=7860
ENVIRONMENT=development

# JWT Authentication Secret Key (Replace with high-entropy secret in production)
JWT_SECRET_KEY=multi_crop_plant_disease_secret_key_2026_dev
JWT_ALGORITHM=HS256
TOKEN_EXPIRATION_SECONDS=604800

# Database Configuration
DB_PATH=database.db

# Model & Gating Configuration
MODEL_CHECKPOINT_PATH=models_assets/model_b_partial_adapted.pth
CONFIDENCE_THRESHOLD=50.0
MAX_UPLOAD_SIZE_MB=10
