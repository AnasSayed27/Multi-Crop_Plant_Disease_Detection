[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: tests requiring local physical benchmark images (deselect with '-m "not integration"')
filterwarnings =
    ignore::DeprecationWarning:passlib.*
    ignore::UserWarning:starlette.*
    ignore::UserWarning:fastapi.*


